# Generated database types

Run the command in `docs/DATABASE.md` after applying migrations. It creates
`database.generated.ts` in this directory.

Do not hand-edit the generated file. Review its diff against the migration and
confirm it contains no connection strings, credentials, or local paths before
committing it.
